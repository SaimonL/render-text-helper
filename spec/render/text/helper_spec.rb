# frozen_string_literal: true

RSpec.describe Render::Text::Helper do
  it "has a version number" do
    expect(Render::Text::Helper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
